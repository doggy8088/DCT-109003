import { SsidValidatorDirective } from './ssid-validator.directive';

describe('SsidValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SsidValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
